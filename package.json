{
  "name": "auto-hot-matrix-publish",
  "private": true,
  "description": "Playwright (CDP) browser publish helper for auto_hot_matrix_publish skill",
  "type": "module",
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
